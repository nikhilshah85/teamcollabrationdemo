import { GendergreetingsPipe } from './gendergreetings.pipe';

describe('GendergreetingsPipe', () => {
  it('create an instance', () => {
    const pipe = new GendergreetingsPipe();
    expect(pipe).toBeTruthy();
  });
});
